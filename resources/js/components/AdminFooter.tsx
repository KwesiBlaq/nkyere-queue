export default function AdminFooter() {
    return (
        <footer className="border-t border-border-subtle/10 px-8 py-4 text-center text-xs text-ink-muted">
            © {new Date().getFullYear()} Nkyere — it won't be long
        </footer>
    );
}
